@model Modellenbureau.Models.Photo

@{
    ViewData["Title"] = "Details";
}

<h1>Details of model</h1>

<div>
    <h4>Photo</h4>
    <hr />
    <dl class="row">
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ImgData)
            @Html.DisplayFor(model => model.ModelUser.Age)
            @Html.DisplayFor(model => model.ModelUser.City)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
